[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-process"
version = "0.4.0"
description = "A one-shot Codex CLI model launcher"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
agent-process = "agent_process:main"

[tool.setuptools]
py-modules = ["agent_process"]
